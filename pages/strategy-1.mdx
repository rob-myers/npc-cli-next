import {
  FontAwesomeIcon,
  faCoffee,
  faChevronRight,
  faLungs,
} from '@/src/components/Icon';

# Strategy: needs and haves

...

export { default } from '../src/components/Root';

export const metadata = {
  key: 'strategy-1',
  date: '2024-09-18',
  info: 'Strategy ',
  giscusTerm: '/strategy-1',
  label: 'strategy-1',
  path: '/strategy-1',
  tags: ['cli', 'web dev', 'behaviour', 'video games'],
}

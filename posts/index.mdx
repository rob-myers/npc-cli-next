export const metadata = {
  key: 'index',
  date: '2025-04-05',
  info: 'Blog Home page',
  giscusTerm: '/blog',
  label: 'blog-home',
  path: '/',
  tags: ['cli', 'web dev', 'npc behaviour', 'video games'],
};

import Card from "../components/Card";
import Carousel from '../components/Carousel';
import Code from '../components/Code';
import Figure from '../components/Figure';
import { faGrip, faExpandThin, faChevronRight } from '@/npc-cli/components/Icon';
import Image from 'next/image';
import TabsLayoutLink from '@/npc-cli/components/TabsLayoutLink';
import Video from '../components/Video';

import DesktopEmptyWorldWebp from '../public/images/desktop-empty-world__20250612.webp';
import ManyRobsTestWebp from '../public/images/many-robs-test.webp';
import IdleViewerSmallWiewportWebp from '../public/images/idle-viewer-small-viewport.webp';
import IdleViewerLargeWiewportWebp from '../public/images/idle-viewer-large-viewport.webp';
import ViewerControlsWithPlayCircled from '../public/images/viewer-controls-with-play-circled.webp';

# Home

## Quickstart

Let's spawn, move, and remove an NPC.<SideNote>
A "Non-Player Character" (NPC) is a video game character not controlled by the Player.</SideNote>

<Card background='#eef'>
  Desktop users can follow along,
  mobile users should probably wait.
  <SideNote>Using a terminal on a mobile is a bit tricky, we'll avoid it for now.</SideNote>
</Card>

<Carousel
  height={500}
  heightMobile={300}
  background="#000"
>
  <Video videoKey='desktopDemo2' label="Desktop demo 2" noBg />
  {/* <Video videoKey='mobileDemo' label="Mobile demo" /> */}
  {/* <Video videoKey='mobileDemo' label="Mobile demo" /> */}
</Carousel>

{/* The above videos show this website on a laptop or mobile phone.
You can step through the code below using the _Viewer_ (right/bottom of this page). */}

{/* Choose <TabsLayoutLink layoutPresetKey="world-tty-default">desktop layout</TabsLayoutLink> or <TabsLayoutLink layoutPresetKey="world-tty-quickstart">mobile layout</TabsLayoutLink>. */}

To use the same tabs layout <TabsLayoutLink layoutPresetKey="world-tty-default">click here</TabsLayoutLink>.
Next, ensure the _Viewer_ is open (drag or toggle) and press play.

<Figure maxHeight={80}>
  <Image
    src={ViewerControlsWithPlayCircled}
    alt="Viewer controls with play circled"
  />
</Figure>

In the video we work our way through the following commands.
Although not particularly inspiring they're a natural beginning.

<Code>
  ```sh title="Spawn, move and remove" showLineNumbers
  # spawn npc at navigable or actable
  spawn npcKey:rob at:$( click 1 )

  # move rob to navigable
  # assuming doors accessible
  move npcKey:rob to:$( click 1 )

  # respawn rob with skeleton key
  spawn npcKey:rob at:$( click 1 ) grant:.

  # move rob via a waypoint
  move npcKey:rob to:$( click 2 )

  # dust to dust
  w npc.remove rob
  ```
</Code>

🚧 screenshots with labels

Terminals traditionally have a profile -- a script one runs initially.
So far we've only seen the _default profile_ which provides various shell functions.
Let's use a different profile which spawns an NPC, and runs a background process supporting "click to move".
One can follow along on Desktop or Mobile.

<Carousel
  height={500}
  heightMobile={300}
  background="#000"
>
  <Video videoKey='desktopDemo2' label="Desktop demo 2" noBg />
  <Video videoKey='mobileDemo' label="Mobile demo" />
</Carousel>

Let's finish this quickstart with our underlying motivations.

- __NPCs indistinguishable from human-controlled characters.__
  Permitting a human Game Master makes this possible.<SideNote>
  Can one human act indistinguishably from many humans playing individual parts?</SideNote>
  It also motivates realtime control systems.

- __An in-depth explanation of [Recast-Detour](https://github.com/recastnavigation/recastnavigation).__
  Our NPCs are driven by Recast-Detour running as WASM in a web worker.
  We'll analyse this important C++ library.

- __A generic scripting system for [three.js](https://www.npmjs.com/package/three).__
  We interpret [mvdan-sh](https://github.com/mvdan/sh) syntax trees as JavaScript.
  In production one can run commands.
  In development, shell functions and processes are synced with JS modules.<SideNote>Technically this approach is independent of three.js.</SideNote>


## What's next

Our _main_ blog is about creating, combining and controlling NPC behaviour in realtime.
Our _dev_ blog is concerned with the underlying software (internal and external).
They're separated in-line with our underlying motivations.

### `main`

🚧 Multiple NPCs

### `dev`

🚧 Introducing the `World`

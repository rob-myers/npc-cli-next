export const metadata = {
  key: 'index',
  date: '2025-04-05',
  info: 'Blog Home page',
  giscusTerm: '/blog',
  label: 'blog-home',
  path: '/',
  tags: ['cli', 'web dev', 'npc behaviour', 'video games'],
};

import Image from 'next/image';

import Card from "../components/Card";
import Carousel from '../components/Carousel';
import Code from '../components/Code';
import Figure from '../components/Figure';
import { faGrip, faExpandThin, faChevronRight } from '../npc-cli/components/Icon';
import { LaptopIcon, MobileIcon } from '../npc-cli/components/Icons';
import TabsLayoutLink from '../npc-cli/components/TabsLayoutLink';
import Video from '../components/Video';

import DesktopViewerClosedWebp from '../public/images/desktop-viewer-closed.webp';
import DesktopViewerOpenWebp from '../public/images/desktop-viewer-open.webp';
import DesktopViewerPlayButtonWebp from '../public/images/desktop-viewer-play-button.webp';
import MobileTtyTouchMenuWebp from '../public/images/mobile-tty-touch-menu.webp';

# Home

## Quickstart

Let's spawn, move, and remove an NPC.<SideNote>
A "Non-Player Character" (NPC) is a video game character not controlled by the Player.</SideNote>

<Card background='#eef'>
  <LaptopIcon/> Desktop users can follow along.

  <MobileIcon/> Mobile users can watch the video then skip to the mobile section.<SideNote width={200}>Using a terminal on a mobile is a bit tricky.</SideNote>
</Card>

<Carousel
  height={450}
  heightMobile={300}
  background="#000"
>
  <Video videoKey='quickstartAttempt1' label="Quickstart" noBg />
  {/* <Video videoKey='mobileDemo' label="Mobile demo" /> */}
  {/* <Video videoKey='mobileDemo' label="Mobile demo" /> */}
</Carousel>

{/* The above videos show this website on a laptop or mobile phone.
You can step through the code below using the _Viewer_ (right/bottom of this page). */}

{/* Choose <TabsLayoutLink layoutPresetKey="world-tty-default">desktop layout</TabsLayoutLink> or <TabsLayoutLink layoutPresetKey="world-tty-quickstart">mobile layout</TabsLayoutLink>. */}

<Card background='#eef'>
  <LaptopIcon/> To follow along, open the Viewer: its on the right side of the screen and can be dragged or toggled.
  Click the _play button_ shown in the slides below.
  Finally ensure the same tabs by clicking <TabsLayoutLink layoutPresetKey="world-tty-default">here</TabsLayoutLink>.

  <Carousel
    height={400}
    heightMobile={250}
    border='1px solid #aaa'
    navigation
    background='#559'
  >
    <Figure label="Viewer closed">
      <Image src={DesktopViewerClosedWebp} alt="Desktop Viewer closed"/>
    </Figure>
    <Figure label="Viewer open">
      <Image src={DesktopViewerOpenWebp} alt="Desktop Viewer open"/>
    </Figure>
    <Figure label="Viewer play button" objectFit="contain">
      <Image src={DesktopViewerPlayButtonWebp} alt="Desktop Viewer play button"/>
    </Figure>
  </Carousel>
</Card>

The video works through the commands below.
They're a natural place to start.

<Code>
  ```sh title="Spawn, move and remove" showLineNumbers
  # spawn npc at navigable or actable
  spawn npcKey:rob at:$( click 1 )

  # move rob to navigable
  # assuming doors accessible
  move npcKey:rob to:$( click 1 )

  # respawn rob with skeleton key
  spawn npcKey:rob at:$( click 1 ) grant:.

  # move rob via a waypoint
  move npcKey:rob to:$( click 2 )

  # dust to dust
  w npc.remove rob
  ```
</Code>


## Mobile Devices

{/* Terminals traditionally have a profile (initial script).
The _default profile_ provides various shell functions.
Let's use a specific profile which (a) spawns an NPC, (b) runs a background process "click to move".
Follow along on Desktop or Mobile. */}

Although the desktop and mobile versions of this site have the same features<SideNote>Supporting touch screens makes sense for tablets e.g. iPads.</SideNote>,
it's harder to use a terminal on a mobile.

> <MobileIcon/> The keyboard hogs space,
predictive text is unhelpful,
there are auto-punctuation conflicts,
and usually no arrow keys.

Mobile screens are too small.
Ideally you should be able to see this blog, a 3D world, and multiple terminals, all at once.
We provide a "touch menu" on the right of terminals to make e.g. pasting, submitting, cancelling commands easier.
But stepping through commands on the blog, terminal, _and_ 3D view is cumbersome.

{/* Technically one can:
- copy commands from the blog and paste them into the terminal on your mobile using  */}

🚧

<Carousel
  height={400}
  heightMobile={30}
  border='1px solid #aaa'
  navigation
  background='white'
>
  <Figure label="TTY touch menu" objectPosition='100% 0%'>
    <Image src={MobileTtyTouchMenuWebp} alt="TTY touch menu"/>
  </Figure>
</Carousel>


<Carousel
  height={450}
  heightMobile={300}
  background="#000"
  navigation
>
  <Video videoKey='mobileDemo' label="Mobile demo" noBg />
  <Video videoKey='desktopDemo2' label="Desktop demo 2" noBg />
</Carousel>

## Motivations

Let us list our underlying motivations.

- __NPCs indistinguishable from human-controlled characters.__
  Permitting a human Game Master makes this tractable.
  Can one human act indistinguishably from many humans playing individual parts?
  <SideNote>It also motivates realtime control systems.</SideNote>

- __Understand [Recast-Detour](https://github.com/recastnavigation/recastnavigation).__
  Our NPCs are driven by Recast-Detour.
  We'll analyse this important C++ library.

- __Generic scripting for [three.js](https://www.npmjs.com/package/three).__
  We interpret [mvdan-sh](https://github.com/mvdan/sh) syntax trees as JavaScript.
  In production one can run commands.
  In development, shell functions and processes are synced with JS modules.<SideNote>Technically this approach is independent of three.js.</SideNote>


## What's next

We provide two blogs.
Our main blog is about creating, combining and controlling NPC behaviour in realtime.
Our dev blog is concerned with the underlying software (internal and external).
They're separated in-line with our underlying motivations.

### `main`

🚧 Multiple NPCs

### `dev`

🚧 Introducing the `World`
